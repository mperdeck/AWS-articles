<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title></title>
</head>
    <body>
        <p>Version 1.35 - 
            server ip:
            @Request.ServerVariables["LOCAL_ADDR"]
            |
            @Html.ActionLink("List Books", "Index", "Books")
            |
            @Html.ActionLink("Add Book", "Add", "Books")
        </p>
        @RenderBody()
        <hr />
        <p>
            This is version: @System.Configuration.ConfigurationManager.AppSettings["SiteVersion"]
        </p>
    </body>
</html>
